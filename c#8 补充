while(循环条件)
{
循环体；
}
条件不成立时，跳出循环
通常要设定一行代码使代码跳出循环
using System;

namespace practice_c//命名空间
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("请输入用户的分数0-100");
            int grade = Convert.ToInt32(Console.ReadLine());
            switch(grade/10)
            {
                case 10:Console.WriteLine("A");
                    break;
                case 9:Console.WriteLine("A");
                    break;
                case 8:Console.WriteLine("B");
                    break;
                case 7:Console.WriteLine("C");
                    break;
                case 6:Console.WriteLine("D");
                    break;
                default:Console.WriteLine("不及格，垃圾");
                    break;
            }
        }
    }
}//用switch-case表示分段情况
