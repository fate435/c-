using System;

namespace ConsoleApp2
{
    class Program
    {
        static void Main(string[] args)
        {
            int n=Program.GetMax(8,68);
            Console.WriteLine(n);
        }
        /// <summary>
        /// 计算并返回两个参数的较大值
        /// </summary>
        /// <param name="x">第一个参数</param>
        /// <param name="y">第二个参数</param>
        /// <returns>较大值</returns>
        public static int GetMax(int x,int y)
        {
            return x > y ? x : y;
        }
    }
}
//调用一个函数就是调用一段代码，所需要你提供的数据就是函数的参数，得到的结果就是返回值
//方法就是对冗余代码（需要反复调用的完全一样的代码）的重用，提高效率
//声名方法的步骤如下
//[public] static 返回值类型 方法名 （【参数列表】）
//{
//    方法体
//}
//public访问修饰符
//static 静态
//返回值类型：int..string...若不需要返回值，为void
//方法名按Pascal
//方法与main函数同级，写在class下
//return的用处：立即结束方法，返回一个返回值
//调用：类名：方法名（参数）；
